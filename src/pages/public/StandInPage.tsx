import React from 'react';
import { Construction, ArrowRight, Star, Mail, Newspaper } from 'lucide-react';
import { Link } from 'react-router-dom';
import { Button } from '../../components/ui/Button';
import { useEffect, useState } from 'react';
import { useAuthStore } from '../../store';

export const StandInPage: React.FC = () => { const [subscribed, setSubscribed] = useState(false); const logout = useAuthStore((state) => state.logout); const tabs = [['Marketplace', '/marketplace'], ['Security', '/security'], ['How It Works', '/how-it-works']]; useEffect(() => { logout(); }, [logout]); return <div className="min-h-[70vh] px-4 py-12"><div className="max-w-5xl mx-auto space-y-10"><nav className="grid grid-cols-3 gap-2 bg-slate-900 border border-slate-800 rounded-2xl p-2" aria-label="KEYStone resources">{tabs.map(([label, href], index) => <Link key={href} to={href} className={`text-center py-3 rounded-xl text-sm font-bold transition-colors ${index === 0 ? 'bg-blue-600 text-white' : 'text-slate-400 hover:bg-slate-800 hover:text-white'}`}>{label}</Link>)}</nav><div className="text-center space-y-4"><div className="w-16 h-16 rounded-2xl bg-amber-500/10 border border-amber-500/30 text-amber-400 flex items-center justify-center mx-auto"><Construction className="w-8 h-8" /></div><h1 className="text-4xl font-black text-white">KEYStone resource center</h1><p className="text-sm text-slate-400">Explore the trust protocol, meet verified talent, and keep up with the next release.</p><Link to="/client/projects/new"><Button size="lg" rightIcon={<ArrowRight className="w-4 h-4" />}>Start a Project</Button></Link></div><section className="grid md:grid-cols-3 gap-4"><div className="bg-slate-900 border border-slate-800 rounded-2xl p-5"><Star className="w-5 h-5 text-amber-400" /><h2 className="text-base font-bold text-white mt-3">Review feedback</h2><p className="text-sm text-slate-300 mt-3">“Clear checkpoints and fast, fair payout decisions.”</p><p className="text-xs text-slate-500 mt-2">Vikram, verified client</p></div><div className="bg-slate-900 border border-slate-800 rounded-2xl p-5"><Mail className="w-5 h-5 text-blue-400" /><h2 className="text-base font-bold text-white mt-3">Contact us</h2><p className="text-xs text-slate-400 mt-2">Questions about custody or governance?</p><a className="text-sm text-blue-400 mt-3 block" href="mailto:support@keystone.demo">support@keystone.demo</a></div><div className="bg-slate-900 border border-slate-800 rounded-2xl p-5"><Newspaper className="w-5 h-5 text-emerald-400" /><h2 className="text-base font-bold text-white mt-3">Newsletter</h2>{subscribed ? <p className="text-sm text-emerald-400 mt-3">You are subscribed.</p> : <form onSubmit={(event) => { event.preventDefault(); setSubscribed(true); }} className="mt-3 space-y-2"><input required type="email" placeholder="you@example.com" className="w-full bg-slate-950 border border-slate-800 rounded-xl p-2.5 text-xs text-white" /><Button type="submit" size="sm">Subscribe</Button></form>}</div></section></div></div>; };
